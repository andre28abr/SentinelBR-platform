VM_NAME=lab-fedora
DISTRO=fedora
ARCH=arm64
PKG_MGR=dnf
DISPLAY_NAME="Lab Fedora"
HOSTNAME=lab-fedora.orb.local
AGGRESSIVE=false
INSTALL_CLAMAV=true
# Fedora vira a showcase da aba "Ferramentas" — instala TODAS as Tier 1+2.
# Outras distros nao mexem (ubuntu so ClamAV, debian-11 puro, vuln-lab puro).
INSTALL_HARDENING_TOOLS=true
