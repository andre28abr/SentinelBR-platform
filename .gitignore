# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editores
.idea/
.vscode/*
!.vscode/settings.json.example
*.swp
*.swo

# Claude
.claude/

# Go (agent)
agent/bin/
agent/dist/
agent/coverage.out
agent/*.test

# Python (server)
server/.venv/
server/__pycache__/
server/**/__pycache__/
server/*.egg-info/
server/.pytest_cache/
server/.mypy_cache/
server/.ruff_cache/
server/htmlcov/
server/.coverage
*.pyc

# Node / web
web/node_modules/
web/dist/
web/.turbo/
web/coverage/
web/*.log

# Deploy / runtime
deploy/data/
deploy/.env
*.local.env
# JWT secret gerado pra dev local pelo start-dev.command
.env-dev

# Protobuf gerado (regerar via `make proto`)
proto/gen/

# Logs
*.log

# Secrets
*.pem
*.key
*.crt
!*.example.key
posts-linkedin/

# CA + server certs gerados (ficam em server/data/, regenerados na primeira execucao)
server/data/

# Celery beat schedule files (gerados em runtime)
server/celerybeat-schedule*

# Docs de planejamento — mantidos local enquanto o projeto nao termina.
# Versao polida vai voltar pro repo no fim.
docs/
samples/labs/bin/
